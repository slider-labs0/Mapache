__pycache__/
*.pyc
.env
.venv/
venv/
*.log

# Runtime data, not source
mapache.json
.mapache/
plugins/generated/
plugins/archived/
engagements/
knowledge/
opplan.json
scope.json
vaccines/

# Packaging build artifacts
*.egg-info/
build/
dist/
learning.json

# User-specific MCP server lists (copy from mcp.json.example). Keep the example tracked.
# e.g. mcp.json (surface web) + mcp-tor.json (Tor-routed).
mcp.json
mcp-tor.json

# External benchmark repos / tools cloned alongside the project (not part of Mapache)
/cybench/
/validation-benchmarks/
/OmniRoute/
/PurpleLlama/

# Demo/runtime artifacts (not part of Mapache)
.playwright-mcp/
qwentest.py
