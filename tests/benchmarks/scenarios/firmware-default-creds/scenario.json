{
  "id": "firmware-default-creds",
  "discipline": "iot",
  "title": "Hardcoded root credentials in extracted router firmware",
  "difficulty": "medium",
  "win_condition": "finding",
  "target": {"kind": "analysis"},
  "objective": "You are analyzing a filesystem extracted from an IoT device's firmware image (artifacts directory). Identify the weakness that gives an attacker device access, cite the exact file and value as evidence, name the class (with CWE), and recommend the fix.",
  "artifacts_dir": "artifacts",
  "tags": ["iot", "firmware", "default-credentials", "hardcoded"],
  "rubric": {
    "cwe": "CWE-798",
    "vuln_keywords": ["hardcoded", "hard-coded", "default credential", "backdoor account", "hardcoded credential", "hardcoded password", "use of hard-coded credentials"],
    "evidence_markers": ["/etc/passwd", "/etc/shadow", "root:", "telnetd", "admin"],
    "grounded_markers": ["root:$1$", "telnetd -l /bin/sh"],
    "remediation_keywords": ["remove", "disable telnet", "unique per-device", "rotate", "no default", "force password change", "disable the account"],
    "min_evidence": 2
  }
}
