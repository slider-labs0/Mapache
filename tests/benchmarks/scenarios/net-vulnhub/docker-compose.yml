# VulnHub-style mini-replica: an app-manager login that still accepts the vendor
# default tomcat/tomcat (CWE-1392). Self-contained flask stand-in.
services:
  web:
    build: .
    ports:
      - "127.0.0.1:0:8080"
    restart: "no"
